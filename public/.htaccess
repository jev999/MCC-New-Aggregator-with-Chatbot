# ============================================
# Security Headers - Hardened Web Application
# ============================================

<IfModule mod_headers.c>
    # Force HTTPS (Strict Transport Security)
    # Tells browsers to only access this site via HTTPS for 1 year
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

    # Prevent Clickjacking - only allow same origin framing
    Header always set X-Frame-Options "SAMEORIGIN"

    # Prevent MIME-type sniffing
    Header always set X-Content-Type-Options "nosniff"

    # Referrer Policy - control what referrer info is sent
    Header always set Referrer-Policy "strict-origin-when-cross-origin"

    # Permissions Policy (formerly Feature Policy)
    # Disable geolocation, microphone, camera for privacy
    Header always set Permissions-Policy "geolocation=(self), microphone=(), camera=(), payment=(), usb=()"

    # X-XSS-Protection for older browsers
    Header always set X-XSS-Protection "1; mode=block"

    # Content Security Policy (CSP) - Customized for MCC News Aggregator
    # Allows Google reCAPTCHA, maps, SweetAlert2, Font Awesome, Google Fonts, Leaflet
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.google.com https://www.gstatic.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://unpkg.com https://nominatim.openstreetmap.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://unpkg.com; font-src 'self' data: https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: https: http:; connect-src 'self' https://www.google.com https://nominatim.openstreetmap.org https://tile.openstreetmap.org; frame-src 'self' https://www.google.com; object-src 'none'; base-uri 'self'; form-action 'self'"

    # Remove Server signature for security
    Header always unset X-Powered-By
    Header unset X-Powered-By

    # Prevent browser caching of sensitive pages
    <FilesMatch "\.(php)$">
        Header set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
        Header set Pragma "no-cache"
        Header set Expires "0"
    </FilesMatch>
</IfModule>

# Directory Listing Protection
Options -Indexes
DirectoryIndex index.php index.html

# Prevent access to hidden files
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Prevent access to sensitive files, but allow index.php
<FilesMatch "(\.(env|ini|conf|config|log|sql|bak|backup|old|tmp|temp)$)">
    Order allow,deny
    Deny from all
</FilesMatch>


# Block access to version control directories
<IfModule mod_rewrite.c>
    RewriteRule ^(\.git|\.svn|\.hg|\.bzr) - [F,L]
</IfModule>

<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Redirect HTTP to HTTPS (optional - uncomment if using HTTPS)
    # RewriteCond %{HTTPS} off
    # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # -------------------------------------------------------------
    # ✅ Hide .php extension from URL (safe & 403-free)
    # Example: /about will load /about.php if it exists
    # -------------------------------------------------------------
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME}.php -f
    RewriteRule ^(.+?)/?$ $1.php [L]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller (index.php)
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
