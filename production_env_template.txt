# MCC-NAC Production Environment Configuration
# Copy this to .env on your production server

APP_NAME="MCC News Aggregator with Chatbot"
APP_ENV=production
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=false
APP_URL=https://mcc-nac.com

# Database Configuration (Update with your remote database details)
DB_CONNECTION=mysql
DB_HOST=your_remote_db_host
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# Session Configuration (Fixed for production)
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_EXPIRE_ON_CLOSE=false
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
SESSION_DOMAIN=.mcc-nac.com

# Cache Configuration
CACHE_STORE=database
CACHE_PREFIX=mcc_nac_cache_

# Queue Configuration
QUEUE_CONNECTION=database

# Mail Configuration (if using email features)
MAIL_MAILER=smtp
MAIL_HOST=your_smtp_host
MAIL_PORT=587
MAIL_USERNAME=your_email
MAIL_PASSWORD=your_email_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@mcc-nac.com
MAIL_FROM_NAME="${APP_NAME}"

# DeepSeek API (if using chatbot)
DEEPSEEK_API_KEY=your_deepseek_api_key

# Security Settings
BCRYPT_ROUNDS=12
LOG_CHANNEL=stack
LOG_LEVEL=error

# File Storage
FILESYSTEM_DISK=public
